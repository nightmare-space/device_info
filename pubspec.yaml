name: device_info
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  image: ^4.0.17
  global_repository:
    git: https://github.com/nightmare-space/global_repository
  get: ^4.1.3
  fl_chart: ^0.68.0
  adb_util:
    git:
      url: https://github.com/nightmare-space/adb_kit.git
      ref: dev
      path: packages/adb_util
  android_api_server_client:
    git:
      url: https://github.com/nightmare-space/android_api_server_client

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.25.7
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
